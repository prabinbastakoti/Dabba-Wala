const table =
  'CREATE TABLE Restaurant (id int NOT NULL AUTO_INCREMENT,name varchar(255) NOT NULL,username varchar(255) NOT NULL, accountType varchar(255) NOT NULL, contact varchar(255) NOT NULL,password varchar(255) NOT NULL,PRIMARY KEY (id));';

module.exports = table;
